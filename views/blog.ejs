<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog</title>
    <%- include('common/links') %>
</head>
<body>
    <%- include('common/nav') %>

    <div class="container mt-3">
        <h1 class="mb-4"><%= blog.title %></h1>
        <div class="card" style="width: 50rem;">
            <img src="<%= blog.imageurl %>" class="card-img-top" alt="Blog Image">
            <div class="card-body">
                <pre class="card-text"><%= blog.content %></pre>
            </div>
            <div class = "container mt-4">
                <img src="<%= blog.createdby.profilepic %>" alt="Profile Picture" class="rounded-circle" style="width: 40px; height: 40px; object-fit: cover;">
                <span class="ms-2"><strong><%= blog.createdby.username %></strong></span>
            </div>
            <div class="container mt-3">
                <h1 class="mb-4">Comments (<%= comments.length %>)</h1>
                <% if (locals.user) { %>
                <form action="/blog/comment/<%= blog._id %>" method="POST">
                    <div class="mb-3">
                        <label for="comment" class="form-label">Add a comment:</label>
                        <textarea class="form-control" id="comment" name="content" rows="3" required placeholder="Type your Comments here"></textarea>
                    </div>
                    <button type="submit" class="btn btn-primary">post</button>
                </form>
                <% } else { %>
                    <p>Please <a href="/user/signin">sign in</a> to post a comment.</p>
                <% } %>
                <% if (comments.length === 0) { %>
                    <p>No comments yet.</p>
                <% } else { %>
                    <div class="list-group mt-3 mb-3">
                        <% comments.forEach(comment => { %>
                            <div class="list-group-item">
                                <div class="d-flex w-100 align-items-center mb-2">
                                    <img src="<%= comment.createdby.profilepic %>" alt="Profile Picture" class="rounded-circle" style="width: 30px; height: 30px; object-fit: cover;">
                                    <h5 class="mb-1 ms-2"><%= comment.createdby.username %></h5>
                                    <small class="ms-auto text-muted"><%= new Date(comment.createdAt).toLocaleString() %></small>
                                </div>
                                <pre class="mb-1"><%= comment.content %></pre>
                            </div>
                        <% }); %>   
                    </div>
                <% } %>
            </div>
        </div>          
    </div>


    <%- include('common/scripts') %>
</body>
</html>