<nav class="navbar navbar-expand-lg navbar-dark site-nav" aria-label="Main navigation">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Blog World</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar" aria-controls="mainNavbar" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>


    <div class="collapse navbar-collapse site-nav" id="mainNavbar">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0 align-items-center">
        <li class="nav-item">
          <a class="nav-link" href="/">Home</a>
        </li>
        <% if (locals.user) { %>
          <li class="nav-item ms-2">
            <a class="nav-link" href="/blog/createnewblog">Publish a blog</a>
          </li>
        <% } %>
      </ul>

      <ul class="navbar-nav ms-auto align-items-center">
        <% if (locals.user) { %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <% if (locals.user.profilepic) { %>
                <img src="<%= locals.user.profilepic %>" alt="avatar" class="nav-avatar me-2" onerror="this.style.display='none'">
              <% } else { %>
                <div class="me-2 nav-avatar-fallback"></div>
              <% } %>
              <span class="username-truncate" title="<%= (locals.user.username || locals.user.name || locals.user.email) %>"><%= (locals.user.username || locals.user.name || locals.user.email) %></span>
            </a>
            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
              <li><a class="dropdown-item" href="/user/profile/<%= locals.user.id %>">Profile Settings</a></li>
              <li><hr class="dropdown-divider"></li>
              <li><a class="dropdown-item text-danger" href="/user/signout">Logout</a></li>
            </ul>
          </li>
        <% } else { %>
          <li class="nav-item me-2">
            <a class="nav-link" href="/user/signin">Signin</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/user/signup">Signup</a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>

<% if (locals.error) { %>
  <div class="container mt-2">
    <div class="alert alert-danger" role="alert"><%= locals.error %></div>
  </div>
<% } %>
